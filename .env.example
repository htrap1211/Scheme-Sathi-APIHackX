# Scheme Sathi — Environment Variables
# Since this is a static HTML app with no backend,
# the API key is entered via the in-app settings modal (⚙️)
# and stored in a JavaScript variable in memory.

# For reference only — add your key through the app UI:
ASI_API_KEY=your_asi1_api_key_here
